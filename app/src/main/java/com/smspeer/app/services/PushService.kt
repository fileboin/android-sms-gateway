package com.smspeer.app.services

import android.content.Context

object PushService {
    @Suppress("UNUSED_PARAMETER")
    fun register(context: Context) {
        // Firebase Cloud Messaging removed — cloud push notifications are disabled
    }
}
